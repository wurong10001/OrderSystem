{
  "name": "ordersystem-cloudflare-workflow",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/workflow.js"
  },
  "devDependencies": {
    "wrangler": "^4.133.0"
  }
}
